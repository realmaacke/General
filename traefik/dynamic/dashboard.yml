http:
  routers:
    traefik-dashboard:
      rule: "Host(`traefik.domain.se`)"
      entryPoints:
        - websecure
      service: api@internal
      middlewares:
        - redirect-to-https
      tls:
        certResolver: le